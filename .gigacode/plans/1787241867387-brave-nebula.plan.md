# Plan: Supabase Integration

## Overview
Integrate Supabase (PostgreSQL-based) into the Spring Boot barbershop backend project. Configure database connection, JPA/Hibernate, and Supabase JWT authentication.

## Credentials
- **Project ref:** `qzcaycblufsrtzudlrol`
- **JWKS URL:** `https://qzcaycblufsrtzudlrol.supabase.co/auth/v1/.well-known/jwks.json`
- **DB Host:** `aws-0-eu-central-1.pooler.supabase.com`
- **DB Port:** `5432`
- **DB Name:** `postgres`
- **DB User:** `postgres.qzcaycblufsrtzudlrol.postgres`
- **DB Password:** `ROOOT112.!!`

## Files to Create/Modify

### 1. Create `src/main/resources/application.yml`
- Supabase PostgreSQL connection (JDBC URL, user, password)
- JPA/Hibernate settings for PostgreSQL
- Supabase JWKS URL for JWT validation
- Telegram bot token placeholder
- Flyway configuration

### 2. Modify `src/main/java/com/github/barbershop/account/security/SecurityConfig.java`
- Replace local RSA key loading with Supabase JWKS-based `JwtDecoder`
- Use `NimbusJwtDecoder.withJwkSetUri()` for token validation
- Remove dependency on `public_key.pem` / `private_key.pem` files
- Keep JWT encoder minimal or adapt for Supabase

### 3. Create `src/main/resources/db/migration/V1__create_users_table.sql`
- Flyway migration based on existing `User` entity
- Columns: id (serial PK), telegram_id (unique), first_name, last_name, username (unique), photo_url, role, created_at, updated_at
- Role enum type creation

### 4. Create `.env.example`
- Document all required environment variables

## Environment Variables to Set
```
SUPABASE_DB_HOST=aws-0-eu-central-1.pooler.supabase.com
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres.qzcaycblufsrtzudlrol.postgres
SUPABASE_DB_PASSWORD=ROOOT112.!!
SUPABASE_JWKS_URL=https://qzcaycblufsrtzudlrol.supabase.co/auth/v1/.well-known/jwks.json
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
```

## Verification Steps
1. `./mvnw clean compile` — verify no compilation errors
2. Run app — verify connection to Supabase DB and JWKS endpoint
3. Test JWT validation with a Supabase-authenticated request
